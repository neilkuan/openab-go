apiVersion: v1
kind: Secret
metadata:
  name: {{ include "quill.fullname" . }}-secrets
  labels:
    {{- include "quill.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $key, $value := .Values.secrets }}
  {{- if $value }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
  {{- end }}
